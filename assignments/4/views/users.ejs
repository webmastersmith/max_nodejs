<%- include('./includes/head.ejs') %> 
<body>
  <h1>Users</h1>
  <% if (users.length >0) { %>
    <ul>
      <% users.forEach((user) => { %>
        <li><%= user %> </li>
      <% }) %>
    </ul>
    <% } else { %>
      <p>No Users</p>
    <% } %>

</body>
</html>